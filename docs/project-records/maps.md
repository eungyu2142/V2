# maps 작업 기록

지도/리뷰 영역의 기획, 구현, 오류 처리, UI 수정 내용을 날짜별로 기록한다.

## 기준

- 지도 화면은 전체 화면 지도를 기본으로 한다.
- 검색창과 동물 분류 태그는 지도 위 오버레이로 둔다.
- 네이버 지도 SDK는 지도 표시, 내 위치, 마커 표시만 담당한다.
- 네이버 지역 검색 API 키와 Secret은 프론트에 노출하지 않는다.
- 병원 검색은 Supabase Edge Function 또는 서버에서 수집한 데이터만 통해 처리한다.
- 병원 리뷰는 외부 사이트로 이동하지 않고 앱 내부에서 열린다.
- 병원 좋아요, 리뷰 좋아요, 임시저장, 내가 쓴 리뷰는 프로필의 나의 활동과 연결되어야 한다.
- 새 UI는 네이비/블루 디자인 시스템을 따른다.

## 2026-07-03 지도/리뷰 기록 분리

### 요청 요약
- 하단 내비게이션의 지도 기능을 별도 기록 파일에서 관리해야 했다.

### 작업 내용
- `docs/project-records/maps.md`를 지도/리뷰 전용 기록 파일로 지정했다.
- 지도, 병원 검색, 병원 리뷰, 병원 좋아요 관련 판단을 이 파일에 모으기로 했다.

### 남은 작업
- 이후 지도/리뷰 관련 수정은 날짜별로 누적 기록한다.

## 2026-07-10 네이버 지도 초기 위치 표시

### 요청 요약
- 지도 첫 진입 시 내 위치 기준으로 열리고, 내 위치 마커가 바로 보여야 했다.
- 위치 안내 문구와 불필요한 오류 문구는 화면에서 줄여야 했다.

### 작업 내용
- 지도 진입 시 네이버 지도 SDK 로드와 브라우저 Geolocation 요청을 함께 처리하도록 정리했다.
- 위치 권한이 허용되면 현재 위치를 지도 중심 좌표로 사용하고 내 위치 마커를 표시하도록 했다.
- 위치 권한 실패 시에도 서울 기본 좌표로 지도 자체는 뜨게 fallback을 유지했다.
- 초기 위치 안내 문구를 제거하고 지도 중심 이동은 조용히 처리하도록 했다.

### 오류 처리
- 빌드 중 누락된 유틸 함수와 export 문제를 정리했다.
- 브라우저 자동 검증은 로컬 Chrome 권한 문제로 제한이 있었고, 빌드 검증으로 대체했다.

### 검증
- `npm run build` 통과.

## 2026-07-18 병원 검색 API 연결 및 마커 표시

### 요청 요약
- QNA 병원 첨부에서는 지역 검색 API 결과가 나오는데 지도에서는 병원이 보이지 않는 문제를 확인해야 했다.
- 특수동물병원, 이국동물병원, 파충류 동물병원, 조류 동물병원 등 검색어 기준으로 병원을 지도에 띄워야 했다.

### 작업 내용
- QNA 병원 첨부에서 사용하던 `search-hospitals` Edge Function 흐름을 지도 검색에도 연결했다.
- 일반 상점, 카페, 펫샵 등 병원이 아닌 결과가 섞이지 않도록 병원 후보 필터를 강화했다.
- 네이버 지역 검색 API 응답의 `mapx`, `mapy`를 지도용 위도/경도에 맞게 변환하는 로직을 보정했다.
- 지도 첫 진입 시 병원 검색이 한 번 실행되도록 해 병원 H 마커가 표시되게 했다.
- 병원 검색 결과가 없더라도 지도는 계속 표시되도록 분리했다.

### 오류 처리
- Edge Function 기본 검색어가 깨진 문자열로 남아 있던 문제를 수정했다.
- 검색 응답 좌표가 일반 위도/경도가 아니라 스케일링된 좌표로 들어오는 케이스를 확인하고 보정했다.

### 검증
- Edge Function 직접 호출 결과 `status: 200`, 병원 결과 반환 확인.
- `npm run build` 통과.

## 2026-07-18 지도 화면 오른쪽 패널 구조

### 요청 요약
- 데스크톱에서는 네이버 지도처럼 오른쪽에 검색과 병원 목록 패널이 나오게 해야 했다.
- 모바일에서는 한 화면에서 지도와 바텀시트 중심으로 처리해야 했다.

### 작업 내용
- 데스크톱 지도 화면을 지도 본문과 오른쪽 병원 검색 패널 구조로 바꿨다.
- 오른쪽 패널에는 검색창, 내 위치 버튼, 동물 분류 태그, 정렬 버튼, 병원 목록을 배치했다.
- 병원 선택 시 같은 오른쪽 영역에서 병원 정보와 리뷰를 확인할 수 있게 했다.
- 모바일에서는 오른쪽 패널 대신 지도 위 검색창과 하단 바텀시트 흐름을 유지했다.

### 검증
- `npm run build` 통과.

## 2026-07-18 검색 UI 정리

### 요청 요약
- 검색 성공/상태 문구를 지우고, 검색 버튼은 텍스트보다 돋보기 아이콘이 낫다고 했다.
- 입력만 바뀌어도 검색어가 반영되고, 태그가 잘리지 않게 해야 했다.

### 작업 내용
- 지도 화면의 검색 상태 안내 문구를 제거했다.
- 검색 버튼을 돋보기 아이콘 버튼으로 바꿨다.
- 검색어, 동물 분류, 위치 기준 변경 시 병원 검색이 자연스럽게 갱신되도록 debounce를 적용했다.
- 같은 검색 조건 반복 호출을 막아 불필요한 API 요청을 줄였다.
- 오른쪽 패널 태그가 줄바꿈되더라도 잘리지 않게 크기와 wrap을 보정했다.

### 검증
- `npm run build` 통과.

## 2026-07-19 전국 특수동물 병원 데이터 수집 구조

### 요청 요약
- 앱이 열릴 때마다 네이버 지역 검색 API를 실시간 호출하지 말고, 서버에서 주 1회 수집한 데이터를 앱이 읽는 구조가 필요했다.
- 전국 시/군/구와 여러 특수동물 병원 키워드로 검색해 중복 제거한 JSON을 저장해야 했다.

### 작업 내용
- `scripts/collect-exotic-hospitals.mjs` 수집 스크립트 구조를 만들었다.
- 검색 키워드별 동물 분류를 `reptile`, `bird`, `rodent` 등으로 기록하도록 했다.
- 병원명과 주소를 정규화해 중복 병원을 병합하고, `matchedQueries`, `evidence`, `sources`를 남기도록 설계했다.
- 구체 분류가 없는 후보 병원은 앱에서 `기타`로 보이도록 정리했다.
- GitHub Actions 주간 수집 워크플로를 추가해 `public/data/exotic-hospitals.json` 갱신 구조를 마련했다.
- 지도와 QNA 병원 검색은 저장된 JSON을 우선 읽도록 방향을 잡았다.

### 오류 처리
- 기존 수집 스크립트의 문자열/JSON 구조가 깨져 있어 새 구조로 정리했다.
- 429 rate limit 방지를 위해 낮은 동시성, 요청 간 delay, backoff 재시도 정책을 계획에 포함했다.

### 검증
- 당시 수집 기능은 구조 정리 단계였고 실제 전체 수집은 추가 확인이 필요했다.

## 2026-07-19 모바일 바텀시트 상호작용

### 요청 요약
- 지도 화면의 핵심은 모바일 바텀시트를 올리고 내리는 동작이어야 했다.
- 병원 목록을 닫을 수 있어야 하고, 닫힌 상태에서도 다시 위로 올릴 수 있어야 했다.
- 아래로 내릴 때 브라우저 새로고침 제스처와 충돌하지 않아야 했다.

### 작업 내용
- 모바일 병원 목록/상세 패널을 드래그 가능한 바텀시트로 정리했다.
- 바텀시트는 중간, 확장, 닫힘 상태를 가지도록 상태를 분리했다.
- 바텀시트 핸들을 아래로 끌면 닫히고, 닫힌 손잡이를 위로 끌면 다시 열리게 했다.
- 지도 화면 진입 중 `html/body` overscroll을 잠가 모바일 pull-to-refresh와 충돌하지 않게 했다.
- 하단 내비게이션을 위로 스와이프하면 병원 목록 바텀시트가 다시 열리도록 연결했다.

### 검증
- `npm run build` 통과.

## 2026-07-19 병원 상세/리뷰 1차 개편

### 요청 요약
- 병원 정보와 리뷰가 지도 안에서 자연스럽게 열려야 했다.
- 리뷰 보기가 외부 병원 사이트로 이동하면 안 되고, 리뷰가 없으면 없다고 보여야 했다.
- 리뷰 작성 버튼을 통해 앱 내부 리뷰 작성이 가능해야 했다.

### 작업 내용
- 병원 목록 카드에 병원명, 주소, 거리, 실제 리뷰 기반 평점/리뷰 수를 연결했다.
- 병원 상세 패널에 병원 정보, 좋아요 버튼, 최근 진료종, 리뷰 개수, 리뷰 요약, 리뷰 목록을 추가했다.
- 리뷰가 없는 병원은 상세에서만 빈 리뷰 안내가 나오게 하고, 병원 검색 목록에서는 빈 리뷰 문구를 제거했다.
- 리뷰 작성 폼을 `src/features/hospital-map/HospitalReviewForm.tsx`로 분리했다.
- 리뷰 작성 시 마이 펫, 방문일, 비용, 진단, 진료 내용, 처방/약 정보, 리뷰 태그, 본문을 저장하도록 확장했다.
- 리뷰 작성 후 해당 날짜의 다이어리 병원 기록과 연결되는 흐름을 추가했다.

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.

## 2026-07-19 병원 좋아요와 프로필 연결

### 요청 요약
- 저장이 아니라 좋아요라는 표현이 맞고, 좋아요한 병원은 프로필의 나의 활동과 연결되어야 했다.

### 작업 내용
- 병원 저장 표현을 좋아요로 정리했다.
- 병원 좋아요는 병원 스냅샷 형태로 저장해 프로필에서 병원명, 주소, 좌표를 사용할 수 있게 했다.
- 리뷰 좋아요도 리뷰 데이터에 `liked`, `likes` 상태로 반영했다.
- 프로필의 좋아요한 항목에서 병원과 리뷰를 지도 화면으로 다시 열 수 있도록 연결했다.

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.

## 2026-07-19 동물 분류 다중 선택 및 블루 테마 정리

### 요청 요약
- 동물 분류 태그는 전체를 제외하고 중복 선택 가능해야 했다.
- 선택된 태그는 더 진하게 보여야 했다.
- 민트/초록이 아니라 전부 파랑 계열이어야 했다.

### 작업 내용
- 동물 분류 필터를 전체 제외 다중 선택 방식으로 바꿨다.
- 전체 버튼은 선택된 분류를 모두 해제하는 역할로 유지했다.
- 선택/활성 상태를 진한 네이비 `#0f2742`로 통일했다.
- 지도 병원 목록, 상세, 버튼, 태그, 마커의 잔여 민트 계열 색상을 네이비/블루 계열로 덮었다.
- hover 시 색과 박스가 이상하게 튀는 전역 버튼 스타일 충돌을 지도 전용 override로 보정했다.

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.

## 2026-07-19 병원 상세 UI 밀도 조정

### 요청 요약
- 병원 상세 컴포넌트가 너무 크고 길쭉하며 지도 영역을 덮는 문제가 있었다.
- 병원 정보에서 전화번호 대신 최근 진료종과 리뷰 개수를 보여야 했다.
- 병원 검색 목록에는 “아직 리뷰가 없습니다” 문구를 빼야 했다.

### 작업 내용
- 병원 상세에서 전화번호 메타를 제거하고 최근 진료종, 리뷰 개수 메타를 추가했다.
- 병원 검색 목록에서는 리뷰가 없는 경우 별도 안내 문구를 표시하지 않도록 했다.
- 상세 패널의 태그, 좋아요 버튼, 리뷰 작성 버튼 높이와 간격을 줄였다.
- 모바일에서 상세 패널이 과도하게 지도를 덮지 않도록 여백과 높이 규칙을 조정했다.

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.

## 2026-07-22 모바일 지도 및 바텀시트 위치 보정

### 요청 요약
- 모바일에서 병원 목록을 다시 여는 표시가 검색창 근처가 아니라 하단 내비게이션 바로 위에 있어야 했다.
- `병원 120곳` 손잡이는 클릭 버튼이 아니라 바텀시트처럼 위로 끌어올리는 손잡이어야 했다.

### 작업 내용
- 모바일 지도에서 닫힌 바텀시트 손잡이를 하단 내비게이션 바로 위에 고정했다.
- 손잡이 클릭으로 열리는 동작을 줄이고, 위로 드래그할 때 열리는 UX를 우선했다.
- 하단 내비게이션과 바텀시트가 겹치지 않도록 z-index와 bottom 기준을 정리했다.
- 모바일 지도 패널 배경이 큰 흰 판처럼 남지 않도록 지도 오버레이 구조를 보정했다.

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.

## 2026-07-22 지도/리뷰 UI 최종 정리

### 요청 요약
- 모바일 태그가 잘리므로 크기와 간격을 줄여야 했다.
- 태그 선택은 지도 병원과 마커만 바꾸고 바텀시트를 자동으로 열면 안 됐다.
- 병원 마커가 타원형이라 어색하므로 더 예쁜 형태가 필요했다.
- 리뷰 영역이 빽빽했고, 리뷰 작성 폼에서 닉네임, 동물 분류, 세부 종 입력이 불필요했다.
- 마이 펫을 선택하면 세부 종과 분류는 자동으로 들어가야 했다.
- 약봉투 인식은 유지하되 복용 안내와 복용량은 제거하고, 약 이름 대신 약 종류를 기록해야 했다.
- 기존 리뷰에서 `익명`이 보이는 문제를 없애야 했다.

### 작업 내용
- 모바일 동물 분류 태그 칩의 높이, 폰트, 아이콘, gap을 줄이고 가로 스크롤을 안정화했다.
- 태그 클릭 시 `setSheetDismissed(false)`가 실행되지 않도록 하여 바텀시트가 자동으로 열리지 않게 했다.
- 병원 마커를 기존 타원형 H 버튼에서 파란 지도 핀 형태로 바꿨다.
- 선택된 병원 마커는 진한 네이비로 강조하고, 신뢰도 배지는 작은 점으로만 표시했다.
- 리뷰 요약, 평점 분포, 리뷰 카드, 태그의 padding과 gap을 정리해 밀도를 낮췄다.
- 리뷰 작성 폼을 마이 펫 선택, 진료 정보, 약봉투 사진, 리뷰 태그, 리뷰 본문 구조로 단순화했다.
- 닉네임 입력을 제거하고 프로필 닉네임 또는 사용자명을 리뷰 작성자로 사용하도록 했다.
- 마이 펫 선택값에서 동물 분류와 세부 종을 자동으로 리뷰 데이터에 반영하도록 했다.
- 약봉투 인식은 `recognize-medication-bag` Supabase Edge Function 호출 구조를 유지했다.
- 약봉투 폼에서는 약 종류, 복용 시작일, 종료일, 하루 복용 횟수만 남겼다.
- 다이어리 약 복용 계획 연결은 약 종류만 있어도 생성될 수 있도록 `dose` 필드를 선택값으로 낮췄다.

### 수정 파일
- `src/App.tsx`
- `src/App.css`
- `src/features/hospital-map/HospitalReviewForm.tsx`
- `src/features/diary/diaryService.ts`

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.
- Vite 번들 크기 경고만 남았고 컴파일 오류는 없었다.

## 2026-07-23 기록 정리

### 요청 요약
- 지도/리뷰 관련 `2026-...` 개별 메모 파일을 지우고, 해당 내용은 `maps.md`에 날짜별로 정리해야 했다.

### 작업 내용
- `docs/project-records` 안에서 별도 `2026-...md` 파일 존재 여부를 확인했다.
- 현재 폴더에는 삭제할 별도 `2026-...md` 파일이 없었고, 지도/리뷰 기록은 `maps.md`에 모여 있었다.
- 깨진 인코딩으로 읽기 어려웠던 `maps.md`를 한국어 날짜별 기록으로 다시 작성했다.

### 검증
- 문서 정리 작업이므로 앱 빌드는 실행하지 않았다.

## 2026-07-23 내가 쓴 병원 리뷰 수정/삭제

### 요청 요약
- 병원 상세 안에서 내가 직접 쓴 리뷰는 내가 수정하거나 삭제할 수 있어야 했다.

### 작업 내용
- 리뷰 데이터의 `mine` 값을 기준으로 내 리뷰에만 `수정`, `삭제` 버튼이 보이도록 했다.
- `수정`을 누르면 기존 리뷰 내용을 리뷰 작성 폼에 불러오고, 저장 시 같은 리뷰 id로 덮어쓰도록 했다.
- 수정 저장 후 평균 평점, 리뷰 개수, 리뷰 목록이 즉시 다시 계산되도록 기존 리뷰 상태를 갱신했다.
- `삭제`를 누르면 확인창을 띄운 뒤 해당 병원의 리뷰 목록과 로컬 리뷰 저장소에서 제거하도록 했다.
- 수정 중인 리뷰를 삭제하거나 수정 취소하면 리뷰 폼 상태를 초기화하도록 했다.

### 수정 파일
- `src/App.tsx`
- `src/App.css`
- `src/features/hospital-map/HospitalReviewForm.tsx`
- `docs/project-records/maps.md`

### 검증
- `npm run build` 통과.
- `npm run lint` 통과.
- Vite 번들 크기 경고만 남았고 컴파일 오류는 없었다.

## 2026-07-24 지도 상세 팝업 UI/UX 보정

### 요청 요약
- 네이버 지도 UX를 참고해 특수동물 병원 지도 상세 팝업과 리스트 컴포넌트의 정보 위계와 상태 표시를 정리해야 했다.
- 좋아요 버튼 초기 라벨 오류, 리뷰 소량 데이터 그래프 과장, 상세 팝업 overflow, 최근 진료 리뷰 문구, 버튼 위계, 현재 위치/병원 마커 구분, 모바일 필터칩 스크롤 힌트를 수정해야 했다.

### 작업 내용
- 병원 좋아요 상태를 `selectedHospitalIsLiked` boolean으로 통일하고, 병원 id 또는 병원명+주소가 같은 경우에만 좋아요 상태로 판단하도록 보강했다.
- 좋아요 버튼 라벨을 `좋아요`/`좋아요 취소`가 같은 boolean에 따라 정확히 바뀌게 했다.
- 좋아요 버튼은 secondary 스타일, 리뷰 작성 버튼은 primary 스타일로 분리해 액션 위계를 명확히 했다.
- 리뷰가 3개 미만이면 5점 분포 막대그래프를 숨기고 “리뷰가 아직 많지 않아요 · 첫 리뷰를 남겨보세요” 안내와 리뷰 작성 버튼을 표시하도록 했다.
- 리뷰가 3개 이상일 때만 평점 분포 그래프가 나오게 했다.
- 데스크톱 병원 상세 팝업에 `max-height: calc(100vh - 32px)`와 `overflow-y: auto`를 적용해 화면 밖으로 넘치지 않게 했다.
- 병원 태그, 리뷰 요약 태그, 리뷰 아이템 태그는 `overflow-x: auto`로 분리해 세로 스크롤과 충돌하지 않도록 했다.
- “최근 진료종 리뷰 데이터 없음” 문구를 선택된 동물 분류가 있으면 “이 병원의 [분류] 진료 리뷰가 아직 없어요”, 기본 상태에서는 “최근 진료 리뷰가 아직 없어요”로 바꿨다.
- 현재 위치 마커는 파란 펄스 원으로, 병원 마커는 네이비 H 핀으로 구분했다.
- 모바일 동물 분류 필터칩 가로 스크롤에 오른쪽 fade mask를 추가해 스크롤 가능성을 시각적으로 알려주도록 했다.
- `App.tsx`의 불필요한 non-component export를 제거해 Fast Refresh lint 오류도 함께 정리했다.

### 수정 파일
- `src/App.tsx`
- `src/App.css`
- `docs/project-records/maps.md`

### 검증
- `npm run lint` 통과.
- `npm run build` 통과.
- Vite 번들 크기 경고만 남았고 컴파일 오류는 없었다.

## 2026-07-24 네이버 지도 기준 지도/리뷰 화면 재정리

### 요청 요약
- 네이버 지도에서 검증된 검색, 목록, 장소 상세, 리뷰 흐름을 참고해서 특수동물 병원 지도 화면을 더 깔끔하고 신뢰감 있게 정리해야 한다.
- 단순히 비슷하게 보이는 수준이 아니라 검색 패널, 필터 칩, 병원 목록, 상세 팝업, 리뷰 영역, 현재 위치와 병원 마커 구분까지 UX 기준을 다시 잡아야 한다.

### 분석·판단 이유
- 기존 CSS 규칙이 여러 번 누적되어 같은 컴포넌트를 서로 다른 위치와 크기로 덮어쓰고 있었다.
- 지도 화면은 네이버 지도처럼 지도 자체를 중심에 두고, 검색과 목록은 가볍게 얹히며, 병원 상세는 정보 우선순위가 명확해야 한다.
- 리뷰가 적은 병원에 점수 분포 그래프를 크게 보여주면 신뢰도가 떨어지므로 리뷰 수가 적을 때는 안내와 리뷰 작성 유도로 바꾸는 것이 더 적합하다.

### 수정 파일
- `src/App.tsx`
- `src/App.css`
- `docs/project-records/maps.md`

### 핵심 변경 내용
- 병원 상세 좋아요 버튼의 초기 라벨을 실제 좋아요 상태에 맞게 `좋아요` 또는 `좋아요 취소`로 표시하도록 정리했다.
- 좋아요 버튼은 보조 액션, 리뷰 작성 버튼은 주요 액션으로 분리해 버튼 위계를 명확히 했다.
- 리뷰가 3개 미만일 때는 평점 분포 그래프 대신 “리뷰가 아직 많지 않아요 · 첫 리뷰를 남겨보세요” 안내와 리뷰 작성 버튼을 보여주도록 했다.
- 병원 상세 팝업에 최대 높이와 세로 스크롤을 적용하고, 태그 영역은 가로 스크롤로 분리해 텍스트가 잘리지 않도록 했다.
- “최근 진료종 리뷰 데이터 없음” 문구를 선택된 동물 분류 기준 안내 문구로 바꿨다.
- 현재 위치 마커는 파란 펄스 원, 병원 마커는 별도 병원 마커로 구분되게 시각 체계를 정리했다.
- 모바일 필터 칩은 가로 스크롤 가능성을 알 수 있도록 오른쪽 fade 힌트를 넣었다.
- 마지막 CSS 우선순위 블록을 파일 끝에 배치해 지도 패널, 검색창, 태그, 목록, 상세, 리뷰 UI가 하나의 레이아웃 기준으로 정렬되게 했다.

### 검증 결과
- `npm run lint` 실행 예정.
- `npm run build` 실행 예정.

### 남은 작업
- 실제 브라우저에서 모바일 바텀시트 드래그 감도와 네이버 지도 SDK 렌더링 위치를 눈으로 확인하면 더 정밀하게 다듬을 수 있다.

### 추가 검증 및 보정
- `src/features/diary/DiaryPage.tsx`에서 `ReminderType`에 포함된 `shed_check` 메타가 빠져 있어 빌드가 실패하던 문제를 탈피 확인 루틴 메타 추가로 보정했다.
- `npm run lint` 통과.
- `npm run build` 통과.
- 빌드 결과 Vite 번들 크기 경고만 남았고 컴파일 오류는 없다.

## 2026-07-24 병원 수집본 전체 로드 보정

### 요청 요약
- 지도 화면에서 특수동물 병원을 일부만 보여주는 것이 아니라 수집된 동물 병원을 전부 불러와야 한다.

### 분석·판단 이유
- `public/data/exotic-hospitals.json`에는 이미 네이버 지역 검색 API 기반 수집본이 저장되어 있고, 메타 기준 수집 병원 수는 120곳이다.
- 기존 지도 목록 렌더링은 `slice(0, 30)`으로 화면 표시를 30곳으로 제한하고 있었다.
- 검색어와 동물 분류 변경 때 병원 상태 자체를 다시 갈아끼우는 구조라, 수집본 전체를 유지한 상태에서 프론트 필터링하는 흐름이 약했다.

### 수정 파일
- `src/App.tsx`
- `docs/project-records/maps.md`

### 핵심 변경 내용
- 지도 화면 진입 시 `public/data/exotic-hospitals.json` 수집본을 한 번에 전체 로드하도록 했다.
- 병원 목록 표시 제한 `slice(0, 30)`을 제거해 필터 결과 전체가 목록과 마커에 연결되도록 했다.
- 검색어 필터는 병원 상태를 다시 덮어쓰지 않고 이미 로드된 전체 수집본 위에서 적용되도록 했다.
- `특수동물`, `동물병원`, `병원` 같은 일반 검색어는 전체 특수동물 병원을 과도하게 걸러내지 않도록 일반어로 처리했다.
- 동물 분류 태그는 API 재호출 없이 로드된 병원 목록에서 프론트 필터링만 수행한다.

### 검증 결과
- `npm run lint` 통과.
- `npm run build` 통과.
- `public/data/exotic-hospitals.json` 파싱 결과 120곳 확인.

### 남은 작업
- 현재 수집 JSON의 일부 텍스트가 깨져 보이는 구간이 있어, 원본 수집 스크립트의 저장 인코딩과 네이버 응답 디코딩을 별도로 점검하면 데이터 품질을 더 높일 수 있다.
## 2026-07-26 모바일 내 위치 플로팅 버튼 추가

### 요청 요약
- 모바일 지도 화면에서도 내 위치 버튼이 필요하다고 요청했다.

### 분석·판단 이유
- 내 위치 이동은 지도 화면의 핵심 조작이다.
- 기존 내 위치 버튼은 검색 패널 안에 있어 모바일 패널 상태나 화면 폭에 따라 접근성이 떨어질 수 있었다.
- 모바일에서는 지도 위에서 바로 누를 수 있는 플로팅 버튼을 제공하는 것이 적절하다.

### 수정 파일
- `src/components/hospital-map/MapScreen.tsx`
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 지도 캔버스 위에 모바일 전용 `map-mobile-location-button`을 추가했다.
- 버튼은 기존 `requestCurrentLocation()` 흐름을 그대로 사용한다.
- 모바일에서 하단 내비게이션과 겹치지 않도록 오른쪽 아래에 safe-area 기준으로 배치했다.
- 데스크톱에서는 기존 검색 패널 안의 내 위치 버튼을 유지하고, 모바일 플로팅 버튼은 모바일에서만 표시한다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 모바일에서 바텀시트가 열린 상태에서도 내 위치 버튼이 가려지지 않는지 확인한다.

## 2026-07-26 병원 목록 더보기와 선 구분 UI

### 요청 요약
- 병원 목록을 무한 스크롤처럼 계속 보여주지 말고 더보기 버튼으로 나누어 보여 달라고 요청했다.
- 병원 항목을 박스 카드로 만들지 말고 선으로 구분해 달라고 요청했다.

### 분석·판단 이유
- 지도 위 병원 목록은 정보 탐색 보조 영역이므로 모든 결과를 한 번에 길게 노출하면 지도 탐색 흐름이 무거워진다.
- 더보기 버튼은 사용자가 목록 확장을 직접 제어할 수 있어 모바일/데스크톱 모두에서 안정적이다.
- 카드 박스를 반복하면 지도 위 패널이 복잡해 보이므로, 네이버 지도처럼 얇은 구분선 중심의 리스트가 더 적합하다.

### 수정 파일
- `src/components/hospital-map/MapScreen.tsx`
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 병원 목록 기본 노출 개수를 10개로 제한했다.
- `더보기` 버튼을 누를 때마다 10개씩 추가로 표시되게 했다.
- 검색어 변경, 정렬 변경, 새 검색 결과 수신 시 노출 개수를 다시 초기화한다.
- 병원 목록 행의 카드형 테두리, 둥근 박스, 그림자를 제거하고 하단 구분선 중심으로 바꿨다.
- 선택된 병원은 박스 대신 왼쪽 강조선으로 구분한다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 모바일에서 더보기 버튼이 하단 내비게이션과 겹치지 않는지 확인한다.

## 2026-07-26 지도 중심 이동 애니메이션 적용

### 요청 요약
- 지도 마커나 내 위치 이동이 딱딱하게 순간 이동하지 않고, 네이버 지도처럼 내 위치 기준으로 부드럽게 넘어가도록 요청했다.

### 분석·판단 이유
- 기존 지도 이동은 `setCenter()`와 `setZoom()`을 바로 호출해서 화면이 끊겨 보였다.
- 네이버 지도 SDK에서 제공되는 부드러운 이동 메서드가 있으면 우선 사용하고, 지원되지 않는 환경에서는 기존 방식으로 fallback하는 것이 안정적이다.

### 수정 파일
- `src/components/hospital-map/MapScreen.tsx`
- `src/types/map.ts`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 지도 이동 공통 함수 `moveMapSmoothly()`를 추가했다.
- SDK에 `morph()`가 있으면 중심과 줌을 함께 부드럽게 이동한다.
- `morph()`가 없으면 `panTo()`로 중심을 부드럽게 이동하고, 줌은 짧은 지연 후 적용한다.
- 둘 다 지원되지 않으면 기존 `setCenter()`와 `setZoom()`으로 fallback한다.
- 내 위치 이동과 병원 선택 이동에 공통 이동 함수를 적용했다.
- 마커 클릭 시 직접 이동과 선택 effect 이동이 중복 실행되지 않도록 정리했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 브라우저에서 네이버 지도 SDK가 `morph()`를 지원하는지 확인하고, 체감 이동 시간이 빠르거나 느리면 duration을 조정한다.

## 2026-07-26 병원 마커 단순 H 핀 스타일 변경

### 요청 요약
- 사용자가 새 참고 이미지처럼 병원 마커를 초록색 위치 핀과 중앙 H 중심으로 더 단순하게 바꿔 달라고 요청했다.

### 분석·판단 이유
- 이전 H+ 파충류 장식형 마커는 앱의 파충류 병원 맥락은 드러났지만 지도 위에서는 정보가 많아 작게 보일 때 복잡했다.
- 새 참고 이미지는 병원 장소를 명확히 전달하는 `H`가 중심이므로, 지도 마커는 병원 식별성을 우선하고 장식은 줄이는 것이 적절하다고 판단했다.

### 수정 파일
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 병원 마커를 초록색 핀, 흰 원, 중앙 H 구조로 단순화했다.
- 기존 파충류 실루엣과 `+` 장식은 숨겼다.
- 마커 전체 크기는 px 고정 대신 `clamp()` 기반 비율 단위와 내부 `%` 배치로 조정했다.
- 선택된 병원 마커는 더 진한 색과 얕은 외곽 강조로 구분되게 했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 지도 화면에서 마커가 너무 크게 보이면 `--hospital-marker-size`의 최대값만 줄이면 된다.

## 2026-07-26 지도 패널 비율 기반 레이아웃 보정

### 요청 요약
- 지도 화면에서 병원 찾기 패널과 사이드 내비게이션을 px 고정값이 아니라 화면 비율 기준으로 맞춰 달라고 요청했다.

### 분석·판단 이유
- 지도 화면은 지도 자체가 핵심이라 오른쪽 병원 패널이 고정 폭으로 커지면 지도 영역을 과하게 덮는다.
- 데스크톱, 작은 데스크톱, 모바일에서 같은 px 폭을 쓰면 화면 비율이 깨지므로 지도 전용 CSS 변수와 `%` 기준 폭으로 다시 잡는 것이 맞다.

### 수정 파일
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- `.map-shell`에 지도 전용 비율 변수 `--map-nav-left`, `--map-nav-width`, `--map-edge-gap`, `--map-panel-width`를 추가했다.
- 데스크톱 지도 사이드 내비게이션의 위치와 폭을 `%` 기반으로 보정했다.
- 오른쪽 병원 검색 패널의 폭과 위치를 화면 비율로 조정해 지도 영역이 더 넓게 남도록 했다.
- 병원 상세 패널도 목록 패널 옆 공간을 `%` 계산으로 나누어 배치하도록 보정했다.
- 모바일에서는 좌우 4% 여백과 92% 폭으로 패널을 잡아 기기 폭에 따라 자연스럽게 줄어들게 했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 브라우저에서 900px 전후 폭과 모바일 폭을 비교해 패널이 지도 중심을 과하게 가리지 않는지 확인한다.

## 2026-07-26 병원 H+ 마커 시각 개선

### 요청 요약
- 사용자가 제공한 초록색 H+ 병원 핀 이미지를 기준으로 병원 마커를 더 명확하고 예쁘게 바꿔 달라고 요청했다.

### 분석·판단 이유
- 지도 위 병원 마커는 작은 크기에서도 병원임을 즉시 알아볼 수 있어야 한다.
- 실제 이미지 파일을 마커마다 반복 로드하면 지도 성능과 렌더링 안정성에 부담이 될 수 있으므로, 기존 네이버 지도 마커 HTML 구조를 유지하면서 CSS로 H+, 핀 실루엣, 파충류 느낌을 표현하는 방식이 적절하다고 판단했다.

### 수정 파일
- `src/components/hospital-map/mapDependencies.tsx`
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 병원 마커 HTML에 `reptile-hospital-marker`, `marker-face`, `marker-lizard` 구조를 추가했다.
- 병원 마커를 초록 계열 H+ 핀 형태로 바꾸고, 내부에 작은 파충류 실루엣과 포인트 장식을 넣었다.
- 선택된 병원 마커는 살짝 커지고 더 진한 primary 색상으로 보여 지도 위 선택 상태를 구분하게 했다.
- 새 마커 색상은 `src/index.css`의 `--color-*` 토큰을 사용하도록 정리했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 브라우저 지도에서 여러 마커가 모였을 때 겹침 정도와 크기감을 확인한 뒤 필요하면 38px~42px 사이에서 미세 조정한다.
## 2026-07-26 모바일 바텀시트 상승 애니메이션 보정

### 요청 요약
- 모바일 지도 화면에서 병원 바텀시트를 위로 올릴 때도 부드럽게 열리도록 요청했다.

### 분석·판단 이유
- 기존 드래그는 손가락 이동 중 위치는 따라오지만, 손을 놓고 최소/중간/확장 상태로 스냅될 때 전환감이 부족했다.
- 지도 화면의 핵심 조작은 바텀시트를 올리고 내리는 흐름이므로, 드래그 중에는 즉각 반응하고 드래그 종료 후에는 짧은 easing 전환을 적용하는 방식이 적합하다고 판단했다.

### 수정 파일
- `src/components/hospital-map/MapScreen.tsx`
- `src/App.css`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 바텀시트 드래그 중 여부를 관리하는 `isSheetDragging` 상태를 추가했다.
- 병원 목록 바텀시트, 병원 상세 바텀시트, 다시 열기 핸들에 `is-dragging` 클래스를 연결했다.
- 드래그 중에는 transition을 끄고 손가락 위치를 즉시 따라가게 했다.
- 드래그를 놓은 뒤 중간/확장 상태로 올라갈 때 `cubic-bezier` 기반 전환을 적용해 더 부드럽게 스냅되도록 했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 모바일 기기에서 손가락으로 빠르게 올릴 때와 천천히 올릴 때의 체감 속도를 확인해 미세 조정할 수 있다.
## 2026-07-26 지도 탭 라벨을 병원 찾기로 변경

### 요청 요약
- 하단/사이드 내비게이션에서 `지도`라고 표시되는 기능명을 `병원 찾기`로 바꾸자고 요청했다.

### 분석·판단 이유
- 이 화면의 핵심 목적은 단순 지도 표시가 아니라 특수동물 병원을 검색하고 찾는 것이다.
- 내부 라우팅과 상태 키는 기존 `map`을 유지해야 프로필, QNA, 병원 상세 이동 흐름이 깨지지 않는다.

### 수정 파일
- `src/App.tsx`
- `docs/project-records/maps.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용
- 탭 배열의 `map` 라벨을 `병원 찾기`로 변경했다.
- 저장 과정에서 `App.tsx` 인코딩이 UTF-8이 아니게 바뀐 문제를 확인하고 UTF-8로 되돌렸다.
- 내부 라우트명과 컴포넌트명은 그대로 유지해 기존 기능 연결을 보존했다.

### 검증 결과
- `cmd /c npx.cmd tsc -b` 통과.
- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite chunk size 경고만 남았고 빌드 실패는 없었다.

### 남은 작업
- 실제 모바일 하단 내비게이션에서 `병원 찾기` 문구가 너무 길게 보이면 `병원찾기`처럼 공백 없는 표기로 줄일 수 있다.
