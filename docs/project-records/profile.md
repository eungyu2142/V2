# profile 작업 기록

프로필, 계정 관리, 프로필 사진, 닉네임, 로그아웃, 계정 삭제, 나의 활동, 좋아요, 임시저장, 내가 쓴 글/리뷰 수정 삭제 관련 작업을 날짜별로 기록한다.

## 기준

- 좋아요, 임시저장, 내가 쓴 글/리뷰 수정 삭제는 프로필의 나의 활동과 연결되어야 한다.
- 나의 활동 항목을 누르면 해당 UI로 이동해야 한다.
- 사용자의 프롬프트로 코드를 변경할 때마다 요청 요약, 판단 이유, 수정 파일, 핵심 변경 내용, 검증 결과, 남은 작업을 한국어로 남긴다.

## 2026-07-03 프로필 작업 기록 파일 생성

### 요청 요약

- 프로필 기능을 별도 문서로 추적하길 요청했다.

### 판단 이유

- 프로필은 계정, 임시저장, 좋아요, 내가 쓴 글, DB 연결이 계속 누적되는 영역이라 별도 기록 파일이 필요했다.

### 수정 파일

- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 및 나의 활동 연결 작업을 누적 기록할 문서를 만들었다.

### 검증 결과

- 문서 생성 작업이라 빌드는 실행하지 않았다.

### 남은 작업

- 이후 프로필 관련 코드 변경 시 이 문서에 날짜별로 이어서 기록한다.

## 2026-07-10 프로필 임시저장 기능 연결

### 요청 요약

- 프로필에 임시저장 기능을 추가하고, 프로필을 클릭했을 때 각 기능을 실제로 이용할 수 있게 연결해 달라고 요청했다.
- DB 연결이 되어 있다면 DB까지 연결되길 요청했다.

### 판단 이유

- 프로필 메뉴가 단순 표시가 아니라 사용자의 활동 허브가 되어야 했다.
- 임시저장은 QNA/나눔 작성 흐름과 Supabase `drafts` 테이블을 함께 연결해야 했다.

### 수정 파일

- `src/App.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- `DraftItem` 타입을 추가했다.
- Supabase `drafts` 테이블을 `loadAppData`, `saveAppData`, `deleteAppData`로 연결했다.
- 로그인 사용자 기준으로 `drafts` 목록을 로드해 프로필 패널에 전달했다.
- QNA 작성 화면 마지막 단계에 `임시저장` 버튼을 추가했다.
- 나눔 작성 화면 마지막 단계에 `임시저장` 버튼을 추가했다.
- 임시저장 시 `draft_type`은 QNA는 `question`, 나눔은 `share_item`으로 저장했다.
- 프로필 패널에 `임시저장` 메뉴를 추가했다.
- 프로필 임시저장 목록에서 각 초안을 등록하거나 삭제할 수 있게 연결했다.
- `내가 쓴 글`, `저장한 병원`, `로그아웃`, `내 정보 수정` 메뉴도 클릭 시 패널 내부 화면이 바뀌도록 연결했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 임시저장 액션명이 `등록`으로 보여 사용자가 이어쓰기 의도를 이해하기 어려울 수 있었다.

## 2026-07-10 프로필 버튼 표시 보정

### 요청 요약

- 프로필과 DB 연결이 완료되었는지 확인했고, 프로필에 버튼이 보이지 않는다고 말했다.

### 판단 이유

- 코드에는 버튼이 렌더링되어 있었지만, 레이아웃과 표시 스타일 때문에 사용자 화면에서 안 보이는 것처럼 느껴질 수 있었다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 사이드바 하단 `side-nav-profile` 버튼이 렌더링되어 있음을 확인했다.
- 프로필 패널의 `임시저장` 메뉴가 렌더링되어 있음을 확인했다.
- `drafts` 테이블 로드/저장/삭제 흐름이 코드상 Supabase helper와 연결되어 있음을 확인했다.
- 한글 라벨이 인코딩 문제로 깨져 보일 수 있어 유니코드 이스케이프 형태로 교체했다.
- `.side-nav-profile`에 `display: grid !important`, `visibility: visible`, `margin-top: auto`를 추가해 사이드바 하단 버튼이 확실히 보이도록 했다.
- `.profile-panel button`에 `display: flex`, `justify-content: space-between`, `width: 100%`를 추가해 `임시저장` 버튼과 개수 배지가 보이도록 했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 프로필 패널의 글자 간격과 DB 연결 화면을 더 정리해야 했다.

## 2026-07-10 프로필 DB 연결 및 레이아웃 수정

### 요청 요약

- 프로필 패널에서 글자 간격과 배치가 이상하다고 지적했다.
- `내 정보 수정`이 실제 DB와 연결되어 있지 않은 것 같다고 말했다.

### 판단 이유

- `.profile-panel button`의 폭 지정 때문에 뒤로 버튼과 제목 배치가 깨졌다.
- `내 정보 수정`은 안내 문구가 아니라 실제 입력 폼과 Supabase `profiles` 테이블 저장 흐름이어야 했다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- `AppProfile` 타입을 추가했다.
- Supabase `profiles` 테이블에서 현재 로그인 사용자의 `username`, `nickname`, `avatar_url`을 로드하도록 연결했다.
- `내 정보 수정` 화면을 실제 입력 폼으로 변경했다.
- 프로필 저장 시 `profiles` 테이블에 `id`, `username`, `nickname`, `avatar_url`을 upsert하도록 연결했다.
- 저장 성공 시 로컬 상태도 갱신하도록 했다.
- 프로필 사진 URL 미리보기 이미지를 추가했다.
- `뒤로` 버튼이 전체 폭을 차지하지 않도록 `.profile-back`의 폭과 flex 동작을 보정했다.
- 프로필 패널 폭을 넓히고 제목 줄바꿈을 막아 `프로필` 글자가 세로로 찢어지지 않게 했다.
- 프로필 입력 필드와 프로필 사진 미리보기 스타일을 추가했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 프로필 사진 입력 방식은 이후 URL 입력보다 파일 선택 방식으로 바꿀 필요가 있었다.

## 2026-07-11 프로필 임시저장 이어쓰기 수정

### 요청 요약

- 프로필의 임시저장 목록에서 초안 액션이 `등록`으로 표시되는 문제를 지적했다.
- 임시저장은 게시가 아니라 작성 화면으로 돌아가서 `이어쓰기`를 해야 한다고 요청했다.

### 판단 이유

- 임시저장의 목적은 바로 등록이 아니라 중단한 작성 흐름을 복원하는 것이다.
- 기존 `publishDraft` 흐름은 사용자의 기대와 맞지 않았다.

### 수정 파일

- `src/App.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 임시저장 목록의 액션명을 `등록`에서 `이어쓰기`로 변경했다.
- 기존 `publishDraft` 흐름을 제거하고 `continueDraft` 흐름으로 교체했다.
- 임시저장 초안을 클릭하면 프로필 패널을 닫고 QNA 또는 나눔 작성 플로우로 이동하도록 연결했다.
- QNA 작성 플로우가 draft payload의 제목, 본문, 사진, 펫, 카테고리, 연결 기록을 초기값으로 복원하도록 했다.
- 나눔 작성 플로우가 draft payload의 제목, 지역, 설명, 사진, 분류, 종, 성별 정보를 초기값으로 복원하도록 했다.
- 이어쓰기 중 다시 임시저장하면 새 초안을 만들지 않고 기존 draft id를 유지해 같은 초안을 갱신하도록 했다.
- 이어쓰기 완료 후 최종 저장하면 기존 draft를 삭제하도록 연결했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- QNA와 나눔 외 작성 화면에도 임시저장을 확장해야 했다.

## 2026-07-11 임시저장 범위 확장

### 요청 요약

- 임시저장이 QNA와 나눔에서만 되고 다른 작성 화면에서는 되지 않는다고 지적했다.

### 판단 이유

- 프로필의 임시저장은 앱 전체 작성 흐름과 연결되어야 한다.
- 마이펫, 다이어리 기록, 병원 리뷰, 알림도 중간 저장과 이어쓰기가 필요했다.

### 수정 파일

- `src/App.tsx`
- `src/features/diary/DiaryPage.tsx`
- `src/features/hospital-map/HospitalReviewForm.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- `DraftKind`에 `pet`, `care_record`, `hospital_review`를 추가했다.
- 프로필 임시저장 이어쓰기가 마이펫 등록, 다이어리 기록, 병원 리뷰 작성 화면으로도 이동하도록 분기했다.
- 마이펫 등록 화면에 `임시저장` 버튼을 추가하고, 이어쓰기 시 기존 입력값을 복원하도록 연결했다.
- 다이어리 기록 작성 화면에 `임시저장` 버튼을 추가하고, 이어쓰기 시 펫, 날짜, 기록 타입, 단계와 입력값을 복원하도록 연결했다.
- 병원 리뷰 작성 폼에 `임시저장` 버튼을 추가하고, 이어쓰기 시 해당 병원 패널과 리뷰 폼을 다시 열도록 연결했다.
- 알림 작성 화면에도 `임시저장` 버튼을 추가하고, 이어쓰기 시 알림 작성 화면을 다시 열도록 연결했다.
- 이어쓰기 완료 후 최종 저장하면 해당 draft를 삭제하도록 연결했다.

### 오류 및 검증

- 1차 빌드에서 `DiaryPage`의 `initialDraft` prop 타입이 `care_record`로 좁혀지지 않아 TypeScript 오류가 발생했다.
- App에서 `care_record` 분기 후 명시 캐스팅해 prop 타입을 맞췄다.
- 이후 알림 draft까지 추가한 뒤 `npm run build`를 다시 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 임시저장 이어쓰기 시 저장된 마지막 작성 단계까지 복원해야 했다.

## 2026-07-11 임시저장 이어쓰기 단계 복원

### 요청 요약

- 프로필 임시저장에서 QNA와 무료분양/나눔을 열 때 저장된 마지막 단계부터 이어서 작성되길 원했다.

### 판단 이유

- 초안은 내용뿐 아니라 사용자가 있던 작성 단계까지 복원되어야 이어쓰기 경험이 자연스럽다.

### 수정 파일

- `src/App.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 임시저장에서 QNA와 무료분양/나눔을 열 때 저장된 마지막 단계부터 시작하도록 `step` 값을 복원했다.
- 진행 게이지의 단계를 클릭하면 원하는 작성 화면으로 바로 이동하게 했다.
- 초안을 다시 저장할 때 현재 단계도 함께 갱신하게 했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 프로필 사진과 게시글 작성자 표시를 연결해야 했다.

## 2026-07-13 프로필 사진, 내가 쓴 글, 좋아요한 목록 수정

### 요청 요약

- 프로필 사진이 URL 입력 방식인데 사진 선택 방식으로 바꿔야 한다고 지적했다.
- `내가 쓴 글`은 목록에만 있지 말고 수정/삭제가 가능해야 하며, 클릭하면 해당 글로 이동해야 한다고 요청했다.
- `저장한 병원` 표현을 `좋아요한`으로 바꾸라고 요청했다.
- 하단 탭이 너무 작으니 조금 더 크게 보이길 요청했다.

### 판단 이유

- 프로필 사진은 사용자가 파일을 직접 고르는 방식이 더 자연스럽다.
- 프로필의 나의 활동은 조회뿐 아니라 열기, 수정, 삭제까지 연결되어야 한다.
- 저장/좋아요 용어는 앱 전체에서 통일되어야 한다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 사진 입력을 URL 입력에서 파일 선택 방식으로 변경했다.
- 선택한 사진은 Data URL로 미리보기와 프로필 저장에 사용했다.
- 프로필 메뉴의 `저장한 병원`을 `좋아요한`으로 변경했다.
- `좋아요한` 화면에 좋아요한 QNA와 나눔 글을 모아 보여주고, 열기 버튼으로 해당 글 화면으로 이동하도록 했다.
- `내가 쓴 글` 목록에 `열기`, `수정`, `삭제` 버튼을 추가했다.
- QNA 글은 프로필에서 열면 QNA 상세 화면으로 이동하게 했다.
- 나눔 글은 프로필에서 열면 나눔 상세 화면으로 이동하게 했다.
- 프로필에서 QNA/나눔 글을 수정하면 기존 글 id를 유지해 저장되도록 했다.
- QNA 수정 저장 시 기존 댓글, 좋아요, 해결 상태가 초기화되지 않도록 보존했다.
- 하단 탭의 높이, 글자 크기, 여백을 키웠다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 병원 자체 좋아요/리뷰 좋아요까지 프로필 `좋아요한`에 넣으려면 지도 병원 카드의 좋아요 상태 저장과 DB 연결을 추가해야 했다.

## 2026-07-13 프로필 사진의 게시글 표시 연결

### 요청 요약

- 프로필 사진이 글 작성자 표시에도 반영되길 원했다.

### 판단 이유

- 사용자가 등록한 프로필 사진은 본인만 보는 설정값이 아니라 게시글 작성자 정보와 연결되어야 한다.

### 수정 파일

- `src/App.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 사진을 다른 사용자가 볼 수 있도록 QNA/나눔 작성 데이터에 작성자 아바타 URL을 저장했다.
- 프로필 사진이 없는 기존 데이터는 이름 첫 글자 표시로 안전하게 대체했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 계정이 바뀔 때 프로필과 개인 데이터가 섞이지 않도록 분리해야 했다.

## 2026-07-16 프로필 계정 표시 및 계정별 데이터 분리

### 요청 요약

- 프로필을 눌렀을 때 `"~~~"님` 형태로 현재 사용자를 보여주길 요청했다.
- 다른 계정으로 로그인했는데 같은 계정처럼 똑같이 로그인/표시되는 오류가 발생했다고 지적했다.

### 판단 이유

- 프로필 화면은 현재 로그인 계정을 명확하게 보여줘야 한다.
- 계정별 데이터가 섞이면 앱 전체 신뢰도가 무너진다.
- Supabase RLS가 완벽하게 분리되지 않는 상황도 대비해 클라이언트에서 `user_id` 필터를 적용해야 했다.

### 수정 파일

- `src/App.tsx`
- `src/lib/appData.ts`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 프로필 패널 상단을 `{닉네임 또는 아이디}님`으로 표시하고 아래에 `프로필` 보조 라벨을 표시하도록 변경했다.
- `loadAppData`에 `userId`와 `scope: 'mine' | 'all'` 옵션을 추가했다.
- 마이펫, 다이어리 기록, 알림, 임시저장은 현재 로그인 사용자 `user_id`로 필터링해서 불러오도록 변경했다.
- QNA와 나눔은 전체 목록을 보되 row의 `user_id`를 기준으로 `mine` 값을 계산하도록 변경했다.
- `내가 쓴 글`은 `mine === true`인 QNA/나눔만 나오도록 변경했다.
- 임시저장 localStorage fallback 키를 `exocare:drafts:{userId}` 형태로 분리했다.
- 로그인 사용자 id가 바뀌면 이전 계정의 펫, 글, 임시저장, 프로필, 열린 패널/작성 상태를 먼저 초기화하고 새 계정 데이터를 다시 불러오도록 했다.
- 프로필 패널이 열린 상태에서 새 프로필 데이터가 도착하면 입력 필드도 새 프로필 값으로 동기화되도록 했다.

### 검증 결과

- `npm run build`를 실행했고 TypeScript 및 Vite 빌드가 통과했다.

### 남은 작업

- 병원 좋아요와 리뷰 좋아요도 프로필 활동으로 묶어야 했다.

## 2026-07-19 프로필 좋아요 관리 연결

### 요청 요약

- 프로필에서 좋아요 관리가 제대로 되지 않는다고 지적했다.
- 지도에서 병원 좋아요를 눌러도 프로필 좋아요 목록에 안정적으로 반영되지 않는 문제가 있었다.
- 좋아요 목록은 QNA 좋아요, 병원 좋아요, 리뷰 좋아요 순서로 정렬되어야 한다고 요구했다.

### 판단 이유

- 프로필의 `좋아요한`은 QNA만 보여주는 메뉴가 아니라 병원과 리뷰까지 포함하는 활동 모음이어야 한다.
- 병원 좋아요는 id만 저장하면 프로필에서 병원명, 주소, 지도 이동을 안정적으로 처리할 수 없다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `src/features/diary/DiaryPage.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 병원 좋아요를 누르면 `HospitalSnapshot` 전체를 저장하도록 변경했다.
- 병원 좋아요 저장 시 기존 id 배열도 함께 갱신해서 과거 로컬 저장 데이터와 호환되게 했다.
- 리뷰 작성/임시저장 payload에 `hospitalName`, `hospitalSnapshot`, `liked`, `likes`를 포함했다.
- 병원 리뷰 카드에 `좋아요 n` 버튼을 추가하고, 클릭 시 `liked`와 `likes`가 로컬 리뷰 저장소 및 상위 앱 상태에 반영되게 했다.
- 프로필의 `좋아요한` 화면은 QNA 좋아요, 병원 좋아요, 리뷰 좋아요 순서로 렌더링했다.
- 병원 좋아요와 리뷰 좋아요는 프로필에서 병원 열기로 지도 화면에 연결되게 유지했다.

### 오류 및 검증

- `npm run build` 1차 실행 시 기존 미사용 코드 때문에 TypeScript 빌드가 실패했다.
- 실패 항목은 `DiaryTimelineSkeleton`, `DiaryTimelineAttachment`, `RecordPicker`, `toAttachedRecordSnapshot`, `togglePlan`이었다.
- 기능 동작을 바꾸지 않도록 명시 참조 처리만 추가해 빌드 차단을 해소했다.
- `npm run build` 최종 통과.
- Vite가 500 kB 이상 chunk 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 프로필 자체를 팝업이 아니라 독립 화면으로 전환해야 했다.

## 2026-07-19 프로필 독립 화면 전환

### 요청 요약

- 기존 프로필 팝업을 계속 보수하는 대신 프로필도 아예 새로운 화면으로 만들자고 요청했다.

### 판단 이유

- 프로필은 내 정보, 내가 쓴 글, 임시저장, 좋아요, 계정 관리가 모두 모이는 화면이라 작은 오버레이보다 독립 화면이 적합했다.
- 사이드바 하단 프로필 버튼은 유지하되 하단 탭에는 넣지 않는 구조가 사용자의 기존 요구와 맞았다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- `Tab` 상태에 `profile`을 추가했다.
- 사이드바 하단 프로필 버튼을 누르면 오버레이가 아니라 `profile` 화면으로 이동하도록 변경했다.
- 하단 네비게이션에는 프로필을 추가하지 않고, 사이드바 하단 버튼으로만 접근하게 유지했다.
- 기존 `ProfilePanel`을 `ProfileScreen`으로 전환하고 어두운 배경/닫기 버튼 구조를 제거했다.
- 내 정보 수정, 내가 쓴 글, 임시저장, 좋아요한, 로그아웃, 계정 삭제 기능은 새 프로필 화면 안에서 그대로 사용할 수 있게 유지했다.
- 프로필 화면에서는 작성 FAB가 뜨지 않도록 제외했다.
- `.profile-screen` CSS를 추가하고 기존 프로필 버튼/입력/목록 스타일을 새 화면에도 적용했다.

### 검증 결과

- `npm run build` 최종 통과.
- Vite가 500 kB 이상 chunk 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 새 프로필 화면의 정보 구조와 시각 디자인을 더 다듬어야 한다.

## 2026-07-23 프로필 작업 기록 정리

### 요청 요약

- 지금까지 한 프로필 작업을 `profile.md`에 날짜별로 기록해 달라고 요청했다.
- `2026`으로 시작하는 별도 파일은 지우고, 그 안의 프로필 관련 텍스트는 `profile.md`로 가져와도 된다고 말했다.

### 판단 이유

- `docs/project-records`에 남아 있던 `2026-*.md` 파일은 현재 확인되지 않았다.
- 기존 `profile.md`에는 필요한 내용이 들어 있었지만 날짜 순서가 섞여 있고 일부 중복이 있어 학습용 문서로 읽기 어렵다.
- 사용자가 공부할 수 있게 요청 요약, 판단 이유, 수정 파일, 핵심 변경 내용, 검증 결과, 남은 작업 형식으로 재정리하는 편이 낫다.

### 수정 파일

- `docs/project-records/profile.md`

### 핵심 변경 내용

- `profile.md`를 날짜순 한국어 작업 기록으로 다시 정리했다.
- 2026-07-03부터 2026-07-23까지 프로필 관련 작업을 한 문서에 모았다.
- 각 날짜 기록에 요청 요약, 판단 이유, 수정 파일, 핵심 변경 내용, 검증 결과, 남은 작업을 붙였다.
- 현재 `docs/project-records`에는 `2026-*.md` 파일이 없어 삭제 작업은 수행할 대상이 없었다.

### 검증 결과

- 문서 정리 작업이라 앱 빌드는 실행하지 않았다.
- `Get-ChildItem docs/project-records -Filter '2026-*.md'` 기준 삭제 대상 파일이 없음을 확인했다.

### 남은 작업

- 앞으로 프로필 코드를 바꿀 때는 이 파일에 같은 형식으로 계속 추가한다.

## 2026-07-23 모바일 프로필 진입 버튼 보정

### 요청 요약

- 프로필이 모바일에서 보이지 않으니 우측 상단에 있게 하자고 요청했다.
- 이어서 지도 화면에서는 프로필 버튼을 띄우지 말라고 정정했다.

### 판단 이유

- 데스크톱에서는 사이드바 하단 프로필 버튼이 보이지만, 모바일에서는 사이드바가 숨겨져 프로필 진입 경로가 부족했다.
- 지도 화면은 전체 화면 지도 UX가 핵심이므로 우측 상단 프로필 버튼이 지도 조작 UI와 겹치면 안 된다.

### 수정 파일

- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- 모바일 전용 `.mobile-profile-button`을 추가했다.
- 버튼은 `activeTab !== 'map'`일 때만 렌더링되도록 해서 지도 화면에서는 보이지 않게 했다.
- 모바일에서만 우측 상단에 고정되고, 데스크톱에서는 숨겨지도록 CSS를 추가했다.
- 프로필 사진이 있으면 사진을 보여주고, 없으면 닉네임/아이디 앞 글자 또는 `ME`를 원형 배지로 보여주게 했다.
- 프로필 화면에 있을 때는 active 상태 스타일이 적용되도록 했다.

### 검증 결과

- `npm run build` 통과.
- Vite가 chunk 크기 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 실제 모바일 화면에서 버튼이 하단 네비 및 각 화면 콘텐츠와 겹치지 않는지 시각 확인이 필요하다.

## 2026-07-23 프로필 버튼 재클릭 시 이전 화면 복귀

### 요청 요약

- 프로필을 한 번 더 누르면 다시 전 화면으로 돌아가게 해 달라고 요청했다.

### 판단 이유

- 모바일 우측 상단 프로필 버튼과 사이드바 하단 프로필 버튼은 단순 이동 버튼보다 토글 버튼처럼 동작하는 편이 자연스럽다.
- 프로필 화면에서 닫기 버튼이 따로 없으므로 같은 프로필 버튼을 다시 눌렀을 때 직전 화면으로 복귀해야 한다.

### 수정 파일

- `src/App.tsx`
- `src/features/diary/DiaryPage.tsx`
- `docs/project-records/profile.md`

### 핵심 변경 내용

- `previousContentTabRef`를 추가해 프로필 진입 직전 탭을 기억하도록 했다.
- `toggleProfileTab` 함수를 추가했다.
- 프로필이 아닌 화면에서 프로필 버튼을 누르면 현재 탭을 저장한 뒤 `profile` 탭으로 이동한다.
- 프로필 화면에서 프로필 버튼을 다시 누르면 저장된 직전 탭으로 돌아간다.
- 모바일 우측 상단 프로필 버튼과 사이드바 하단 프로필 버튼 모두 같은 토글 함수를 사용하게 했다.
- 빌드 중 다이어리 파일의 기존 루틴 추천 목록/기록 단계 타입 오류와 JSX 구문 오류가 드러나 최소 수정으로 정리했다.

### 검증 결과

- `npm run build` 통과.
- Vite가 chunk 크기 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 실제 모바일에서 프로필 버튼을 두 번 눌렀을 때 지도 외 화면 복귀가 기대대로 보이는지 시각 확인이 필요하다.

## 2026-07-26 프로필 화면 비율 레이아웃 보정

### 요청 요약

- 프로필 화면에서 사이드바와 본문이 겹쳐 보이는 문제를 지적했다.
- 고정 px가 아니라 `%` 기반으로 비율을 맞췄어야 한다고 말했다.

### 판단 이유

- 기존 프로필 화면은 일반 페이지 padding과 사이드바 폭 계산이 서로 다른 단위로 섞여 있었다.
- 화면 폭이 줄어드는 데스크톱 구간에서 본문 시작점이 사이드바 폭을 충분히 피하지 못했다.
- 프로필은 독립 화면이므로 사이드바와 같은 비율 체계에서 본문 폭을 다시 계산해야 했다.

### 수정 파일

- `src/App.css`
- `src/components/hospital-map/MapScreen.tsx`
- `docs/project-records/profile.md`
- `docs/project-records/UI.md`

### 핵심 변경 내용

- 데스크톱에서 사이드바의 위치와 폭을 `%` 기반으로 보정했다.
- 비지도 화면의 좌우 padding도 `%` 기반으로 맞춰 본문이 사이드바 아래로 들어가지 않도록 했다.
- 프로필 화면은 `max-width` 제한을 풀고 사용 가능한 본문 폭을 100% 쓰도록 했다.
- 761px~900px 구간에서 별도 비율을 적용해 좁은 데스크톱 폭에서도 겹침을 막았다.
- 빌드 과정에서 병원 리뷰 폼 prop 누락이 발견되어 약봉투 이미지 관련 prop을 `MapScreen.tsx`에서 연결했다.

### 검증 결과

- `cmd /c npm.cmd run build` 통과.
- Vite가 chunk 크기 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 실제 화면에서 프로필 본문과 사이드바 사이 여백이 너무 넓거나 좁지 않은지 확인하면 된다.

## 2026-07-29 프로필 사진 전역 반영

### 요청 요약

- 프로필에서 사진 선택을 하면 모든 화면에 내 프로필 사진이 들어가야 한다고 지적했다.

### 판단 이유

- 기존 Q&A 목록과 상세는 일부 fallback으로 현재 프로필 사진을 쓰고 있었지만, 과거 글에 `authorAvatarUrl`이 저장되어 있으면 새 프로필 사진보다 옛 사진이 우선될 수 있었다.
- Q&A 댓글과 병원 리뷰에는 작성자 프로필 사진 저장/표시 흐름이 부족했다.
- 사용자가 프로필 사진을 바꾸면 내가 작성자로 표시되는 화면은 저장된 옛 URL보다 현재 로그인 사용자의 최신 프로필 사진을 우선해야 한다.

### 수정 파일

- `src/types/app.ts`
- `src/App.tsx`
- `src/components/qna/QnaScreen.tsx`
- `src/components/hospital-map/MapScreen.tsx`
- `src/App.css`
- `docs/project-records/profile.md`
- `docs/project-records/UI.md`

### 핵심 변경 내용

- `QnaComment`와 `HospitalReview` 타입에 `authorAvatarUrl`을 추가했다.
- Q&A 새 글 작성 시 현재 프로필 사진 URL을 `authorAvatarUrl`로 저장하도록 했다.
- Q&A 목록과 상세에서 내 글은 저장된 옛 사진보다 현재 `profile.avatarUrl`을 우선 표시하도록 했다.
- Q&A 댓글도 작성자 아바타를 표시하고, 내 댓글은 현재 프로필 사진을 우선 표시하도록 했다.
- 댓글 저장 payload에 `authorAvatarUrl`을 포함했다.
- 병원 리뷰 작성 시 현재 프로필 사진 URL을 리뷰 데이터에 저장하도록 했다.
- 병원 리뷰 카드에 작성자 아바타를 추가했고, 내 리뷰는 현재 프로필 사진을 우선 표시하도록 했다.
- 프로필 저장 시 로컬에 떠 있는 내 Q&A 글과 병원 리뷰의 작성자 이름/프로필 사진도 최신 값으로 갱신했다.

### 검증 결과

- `cmd /c npm.cmd run build` 통과.
- Vite가 chunk 크기 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 이미 DB에 저장된 과거 댓글의 프로필 사진까지 영구 갱신하려면 `post_comments` payload 일괄 업데이트 흐름을 추가로 만들 수 있다.

## 2026-08-02 GitHub식 프로필 화면 재구성

### 요청 요약

- GitHub 프로필을 참고해 현재 박스 안에 박스가 들어간 프로필 UI/UX 구조를 모바일과 웹 모두 바꾸자고 요청했다.
- 색은 GitHub를 따라가지 않고 앱의 색상 토큰을 유지하라고 했다.

### 판단 이유

- 기존 프로필 화면은 메뉴 버튼을 누르면 다시 박스형 패널이 열리는 구조라 프로필이 활동 허브라기보다 설정 팝업처럼 느껴졌다.
- GitHub 프로필은 프로필 요약, 탭, 대표 활동, 목록을 분리해 정보 구조가 명확하다.
- 우리 앱도 프로필 사진과 사용자 정보는 별도 요약 영역에 두고, 내가 쓴 글·임시저장·좋아요·계정 관리는 탭으로 나누는 구조가 더 맞다.

### 수정 파일

- `src/components/profile/ProfileScreen.tsx`
- `src/App.css`
- `docs/project-records/profile.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용

- 프로필 화면을 `profile-identity` 요약 영역과 `profile-activity` 활동 영역의 2열 구조로 변경했다.
- 웹에서는 GitHub처럼 왼쪽에 프로필 사진, 이름, 아이디, 통계, 프로필 수정 버튼을 배치했다.
- 오른쪽에는 `Overview`, `내가 쓴 글`, `임시저장`, `좋아요`, `계정` 탭을 배치했다.
- 기존 버튼 메뉴식 홈 화면을 제거하고 Overview에서 주요 활동 수치를 바로 볼 수 있게 했다.
- 목록은 카드 안 카드가 아니라 GitHub 저장소 목록처럼 구분선 기반 리스트로 바꿨다.
- 모바일에서는 프로필 요약이 상단에 오고, 탭은 가로 스크롤 가능한 형태로 유지된다.
- 색상은 `--color-*` 토큰을 사용하고 GitHub 색상은 가져오지 않았다.

### 검증 결과

- `cmd /c npm.cmd run build` 통과.
- Vite가 chunk 크기 경고를 냈지만 빌드 실패는 아니었다.

### 남은 작업

- 실제 모바일에서 탭 스크롤과 계정 폼 입력 영역이 손가락 조작에 충분한지 확인하면 된다.
## 2026-08-02 프로필 화면 정보 구조 전면 정리

### 요청 요약

- 기존 프로필 화면의 거대한 좌측 프로필 영역, Overview, 중복 통계 카드, 의미 없는 설명 문구를 제거하고 사용자가 위에서 아래로 자신의 정보와 활동을 읽을 수 있는 구조로 재설계했다.
- 프로필 요약, 활동 요약, 세부 탭과 목록을 하나의 중앙 정렬 화면 안에 배치하고 `/profile?tab=posts` 같은 URL 상태가 새로고침 후에도 유지되도록 요구했다.

### 분석·판단 이유

- 기존 `ProfileScreen`은 작성 글, 임시저장, 좋아요, 리뷰 수치를 좌측 프로필 영역과 Overview 카드에서 반복 표시하고 있었다.
- 알림 설정이 프로필 요약 영역에 섞여 화면 목적이 흐려졌고, Overview 탭은 실제 목록 탐색과 역할이 겹쳤다.
- 프로필 화면은 설정 메뉴보다 활동 확인과 재진입이 우선되어야 하므로 헤더, 활동 요약, 탭 목록 순서로 재배치하는 것이 적합하다고 판단했다.

### 수정 파일

- `src/components/profile/ProfileScreen.tsx`
- `src/App.tsx`
- `src/App.css`
- `src/components/hospital-map/MapScreen.tsx`
- `docs/project-records/profile.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용

- `Overview` 탭과 중복 통계 UI를 제거하고 `작성한 글`, `임시저장`, `좋아요`, `병원 리뷰`, `설정` 탭으로 재구성했다.
- `ProfileHeader`, `ProfileActivitySummary`, `ProfileTabs`, `ProfilePostList`, `ProfileDraftList`, `ProfileLikeList`, `ProfileReviewList`, `ProfileSettings`, `ProfileEmptyState` 단위로 화면을 분리했다.
- 프로필 이미지는 웹 88px, 모바일 72px 기준으로 줄이고, 닉네임과 `@아이디`를 우선 표시하도록 했다.
- 활동 요약 4개 항목을 실제 목록 수치 기반으로 계산하고 클릭 시 해당 탭으로 이동하며 목록 위치로 스크롤되도록 했다.
- 좋아요 탭은 게시글, 병원, 리뷰 필터를 제공해 좋아요한 항목과 병원을 같은 영역 안에서 구분하도록 했다.
- 병원 리뷰 탭을 추가하고 작성 리뷰 목록, 수정 모달 직접 진입, 삭제 동작을 연결했다.
- 알림 설정을 설정 탭 내부로 이동하고 권한 상태에 따라 `알림 켜기`, `알림 켜짐`, `브라우저 설정에서 변경` 문구가 나오도록 했다.
- `/profile?tab=posts`, `/profile?tab=drafts`, `/profile?tab=likes`, `/profile?tab=reviews`, `/profile?tab=settings` URL을 지원하도록 라우팅을 조정했다.
- 프로필 화면 내부 제목과 공통 상단 헤더가 중복되지 않도록 프로필 탭에서는 공통 헤더를 숨겼다.

### 검증 결과

- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과.
- Vite의 500kB 이상 chunk 경고는 기존 번들 크기 경고이며 빌드 실패는 아니다.

### 남은 작업

- 실제 브라우저에서 로그인 계정별 데이터, 병원 리뷰 수정 진입 위치, 모바일 탭 스크롤 감각을 화면 캡처로 최종 확인하면 좋다.

## 2026-08-02 프로필 레이아웃 깨짐 원인 수정 및 반응형 안정화

### 요청 요약

- 이전 프로필 개편 뒤 데스크톱 화면이 2열로 갈라지고, 소개와 목록 텍스트가 좁은 열에 겹치는 문제를 부모 레이아웃부터 분석해 고쳤다.
- 프로필 헤더, 활동 요약, 탭, 실제 목록을 하나의 문서 흐름으로 정렬하고 360px·768px·1280px에서 검증하도록 요청받았다.

### 분석·판단 이유

- 전역 반응형 CSS가 `.profile-screen`에 데스크톱 2열 `grid-template-columns`를 `!important`로 적용해 제목, 사용자 헤더, 활동 카드, 목록을 서로 다른 열로 분산시키고 있었다.
- 또 다른 전역 카드 규칙이 `.profile-list`를 3열 그리드로, 목록 행을 세로 카드로 강제해 긴 제목과 메타데이터의 가용 폭을 줄였다.
- 프로필 전용 DOM이 오래된 공유 클래스명을 계속 사용하면 같은 충돌이 반복되므로 루트, 목록, 더보기 메뉴를 독립된 클래스 계약으로 분리했다.

### 수정 파일

- `src/components/profile/ProfileScreen.tsx`
- `src/App.tsx`
- `src/App.css`
- `docs/project-records/profile.md`
- `docs/project-records/UI.md`
- `docs/project-records/UX.md`

### 핵심 변경 내용

- 프로필 루트를 `profile-page`, 목록을 `profile-content-list`, 더보기 메뉴를 `profile-row-menu`로 변경해 기존 2열·3열 전역 규칙을 제거했다.
- 전체 너비 100%, 최대 너비 1120px, 자연 높이의 단일 문서 흐름으로 제목·헤더·요약·탭·목록의 왼쪽 기준선을 통일했다.
- 프로필 이미지, 닉네임, 아이디, 소개를 하나의 flex 그룹으로 묶고 텍스트 영역에 `min-width: 0`, 소개에 `word-break: keep-all`과 2줄 제한을 적용했다.
- 긴 게시글 제목은 2줄까지만 표시하고 목록은 항상 1열 전체 너비 행으로 유지하며, 더보기 버튼은 40px 고정 클릭 영역으로 분리했다.
- 임시저장에는 눈에 보이는 `이어서 작성` 버튼을 추가했고 글·임시저장·리뷰 삭제에는 브라우저 확인 절차를 넣었다.
- 좋아요한 게시글, 병원, 리뷰의 `좋아요 해제`를 실제 Q&A 상태, 병원 저장소, 리뷰 로컬 상태에 각각 연결했다.
- 탭에 `tablist`, `tab`, `tabpanel`, `aria-selected`를 적용하고 방향키·Home·End 이동을 지원했다.
- 프로필 이미지 로딩 실패 시 닉네임 첫 글자를 보여주고, 프로필 정보 로딩 중에는 헤더 크기를 유지하는 스켈레톤을 표시한다.

### 검증 결과

- `cmd /c npm.cmd run lint` 통과.
- `cmd /c npm.cmd run build` 통과. 기존 Vite 500kB 초과 번들 안내만 남았다.
- 360px: 페이지 가로 오버플로 없음, 활동 요약 2열, 목록 1열, 소개 `keep-all`, 더보기 40px 확인.
- 768px: 활동 요약 2열, 목록 1열, 사용자 헤더 가로 배치 확인.
- 1280px: 프로필 콘텐츠 1120px 이내, 활동 요약 4열, 목록 1열과 긴 제목·더보기 분리 확인.

### 남은 작업

- 프로필 한 줄 소개를 저장할 DB 컬럼이 현재 `profiles` 테이블에 없어 빈 상태 문구만 표시한다. 소개 편집과 영구 저장은 스키마 확장 뒤 연결해야 한다.
- 병원 리뷰와 병원 좋아요는 현재 프로젝트의 기존 로컬 저장 방식을 유지한다. 계정 간 서버 동기화가 필요하면 `likes`, `hospital_reviews` 테이블 저장 흐름을 별도로 통합해야 한다.
## 2026-08-02 활동 요약 숫자 스트립 정리

- 요청 요약: 프로필 상단의 큰 활동 요약 박스를 작게 줄이고 숫자만 노출하며, 세부 이동은 아래 탭이 중심이 되도록 변경.
- 분석·판단 이유: 기존 요약은 120px 높이의 2행 카드와 모바일 2열 배치 때문에 탭보다 많은 공간을 차지했다. 요약은 수치 확인과 바로가기 역할만 담당하고 활동명과 현재 위치는 탭에서 명확히 보여주는 편이 탐색 비용이 낮다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 활동 요약의 아이콘·항목명·화살표를 화면에서 제거하고 4개의 숫자 버튼을 한 줄로 압축했다. 항목명과 수치는 `aria-label`과 툴팁으로 유지했고, 선택 상태·탭 동기화·URL 상태 변경 기능은 그대로 유지했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-03 설정 진입점 통합과 활동 수치 라벨 보완

- 요청 요약: 프로필 수정과 설정의 중복을 없애고, 숫자 요약에는 각 숫자의 의미를 함께 표시.
- 분석·판단 이유: 프로필 수정 버튼이 설정 탭의 계정 정보 편집과 같은 화면을 열어 진입점만 중복되고 있었다. 숫자만 있는 요약은 공간은 작지만 의미를 탭 위치에 의존해야 하므로 짧은 라벨을 같은 줄에 배치하는 편이 빠르게 이해하기 좋다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 프로필 헤더의 `프로필 수정` 버튼과 관련 props·스타일을 제거했다. 계정 편집은 설정 탭으로 일원화했다. 활동 요약은 `숫자 + 작성한 글/임시저장/좋아요/병원 리뷰`를 한 줄로 표시하되 기존의 작은 높이와 4열 구조를 유지했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-03 좋아요 전체 필터 제거

- 요청 요약: 좋아요 내부의 `전체` 필터를 제거.
- 분석·판단 이유: 게시글·병원·리뷰를 모두 이어 붙이는 전체 화면은 항목 종류 구분을 흐리고, 개별 필터와 역할이 겹쳤다. 버튼만 숨기면 내부 상태가 `all`로 남을 수 있어 상태 타입과 렌더링 조건까지 함께 정리했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 좋아요 필터를 게시글·병원·리뷰 세 종류로 제한했다. 첫 진입 시 좋아요 데이터가 존재하는 첫 카테고리를 자동 선택하며, 선택한 종류의 목록만 표시한다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-03 작성한 글 Q&A·리뷰 카테고리 통합

- 요청 요약: 프로필의 작성한 글을 Q&A와 리뷰 카테고리로 구분.
- 분석·판단 이유: 병원 리뷰를 작성한 글 내부와 상위 탭에 동시에 두면 같은 목록으로 가는 경로가 중복된다. 작성 콘텐츠라는 공통 목적 아래 Q&A와 리뷰를 하위 카테고리로 묶고 상위 병원 리뷰 탭은 제거했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/APP_REQUIREMENTS.md`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 작성한 글 안에 Q&A·리뷰 카테고리와 실제 개수를 추가했다. 병원 리뷰 상위 탭을 제거하고 활동 요약의 Q&A와 병원 리뷰 수치는 각각 해당 카테고리로 바로 이동하게 연결했다. 기존 `/profile?tab=reviews` 주소는 `/profile?tab=posts&category=reviews` 상태로 호환한다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-04 Q&A·리뷰 상단 수치 통합

- 요청 요약: 상단 활동 요약의 Q&A와 병원 리뷰를 `글`로 통합하고, 작성한 글 내부에는 Q&A·리뷰 두 카테고리를 표시.
- 분석·판단 이유: 상단에서 Q&A와 병원 리뷰를 나눠 표시하면서 작성한 글 내부에서도 다시 나누면 같은 분류가 중복된다. 상단은 전체 작성 콘텐츠 수를 빠르게 보여주고, 세부 유형은 작성한 글 안에서 선택하도록 역할을 분리했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 상단 활동 요약을 `글·임시저장·좋아요` 3개로 줄였다. 글 수치는 실제 Q&A 개수와 병원 리뷰 개수의 합으로 계산한다. 작성한 글 내부의 Q&A·리뷰 두 카테고리와 각각의 개수는 유지했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-04 빈 상태 문자 마크 제거

- 요청 요약: 프로필 빈 상태에 표시되는 `병`, `임`, `?`, `♡` 원형 마크 제거.
- 분석·판단 이유: 문자 한 글자를 원형 배경에 넣은 마크는 정보 전달력이 낮고 빈 상태의 제목보다 먼저 시선을 끌었다. 모든 빈 상태가 공통 컴포넌트를 사용하므로 개별 숨김이 아니라 공통 구조에서 제거했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: `ProfileEmptyState`의 icon prop과 렌더링 요소를 제거하고 네 호출부의 문자 값을 삭제했다. 원형 마크 CSS를 삭제하고 제목 위 여백을 정리했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-04 Q&A 답변 상태 색상 표시

- 요청 요약: 프로필 작성 글 목록에서도 `답변 대기`와 `해결` 상태를 색상으로 구분.
- 분석·판단 이유: 기존에는 상태가 조회·댓글 수와 같은 메타 문장으로 표시되어 빠르게 구분하기 어려웠다. 상태만 별도 배지로 분리하고 기존 Q&A 화면과 같은 의미의 색상 체계를 사용했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 공통 `ProfilePostStatus`를 추가해 답변 대기는 Accent, 해결은 Primary 색상 배지로 표시했다. 작성한 Q&A와 좋아요한 Q&A 목록에 동일하게 적용하고 조회·댓글 수는 중립 메타 정보로 분리했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-04 리뷰 별점 시각화

- 요청 요약: 프로필 리뷰 목록의 `별점 5` 같은 문자 표현을 실제 별 표시로 변경.
- 분석·판단 이유: 숫자 문구보다 별 아이콘 배열이 리뷰 평점이라는 의미와 상대적인 점수를 더 빠르게 전달한다. 작성 리뷰와 좋아요한 리뷰가 같은 데이터를 보여주므로 공통 컴포넌트로 통일했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 5점 기준으로 채운 별과 빈 별을 생성하는 `ProfileRatingStars`를 추가했다. 화면에서는 별만 보여주고 스크린리더에는 실제 점수와 5점 만점 정보를 제공한다. 작성 리뷰와 좋아요한 리뷰 목록에 모두 적용했다.
- 검증 결과: ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-04 설정 화면 정보 구조 전면 개편

- 요청 요약: 설정을 프로필, 돌봄 알림, 계정, Danger Zone 네 카드로 나누고 현대적인 설정 UX와 반응형 레이아웃을 적용.
- 분석·판단 이유: 기존 화면은 프로필 편집, 알림, 로그아웃, 탈퇴가 밑줄형 섹션과 하나의 계정 영역에 섞여 있어 현재 상태와 위험 기능의 경계가 약했다. 프로젝트에는 Tailwind가 설치되어 있지 않고 기존 화면이 `App.css` 디자인 토큰을 사용하므로 전역 빌드 체계를 추가하지 않고 같은 결과를 토큰 기반 CSS로 구현했다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/APP_REQUIREMENTS.md`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: `ProfileSection`, `NotificationSection`, `AccountSection`, `DangerZoneSection`으로 분리했다. 사진 클릭 업로드, 읽기 전용 아이디, 실제 변경 시에만 저장 활성화, push 구독·해제 스위치, 동일 크기 계정 버튼, 확인 문구 기반 탈퇴 활성화를 연결했다. 설정 콘텐츠는 최대 820px, 카드 간격 24px, 입력·버튼 높이 46px로 통일했다.
- 검증 결과: ESLint와 프로덕션 빌드를 통과했다. 개발 서버는 `http://127.0.0.1:5173/`에서 실행했다. 브라우저 자동 검증은 로컬 주소가 브라우저 보안 정책에 차단되어 진행하지 못했다.
- 남은 작업: 비밀번호 변경은 기존 이벤트와 API가 없어 기존처럼 비활성 상태다. 실제 기능 연결 시 버튼 이벤트를 추가해야 한다.
## 2026-08-05 모바일 설정 탭 노출

- 요청 요약: 모바일 프로필 탭에서 화면 밖으로 밀린 `설정`을 항상 보이게 수정.
- 분석·판단 이유: 모바일에서도 데스크톱용 탭 간격과 고정 폭을 유지해 다섯 번째 설정 탭이 오른쪽으로 잘렸다. 다섯 탭의 중요도가 같으므로 모바일 너비를 동일한 5열로 배분했다.
- 수정 파일: `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 760px 이하에서 프로필 탭을 5열 그리드로 전환하고 간격을 제거했다. 각 탭은 폭 100%, 최소 폭 0, 13px 글자와 줄바꿈 금지를 적용해 설정까지 한 화면에 표시한다.
- 검증 결과: 프로필 파일 대상 ESLint와 프로덕션 빌드는 통과했다. 전체 ESLint는 이번 작업과 무관한 `src/components/hospital-map/MapScreen.tsx:279`의 기존 `react-hooks/set-state-in-effect` 오류로 실패했다.
- 남은 작업: 없음.
## 2026-08-05 활동 요약 4항목 한 줄 배치

- 요청 요약: 글·임시저장·좋아요·채택 답변 요약이 모바일에서 두 줄로 내려가지 않도록 크기를 줄여 한 줄로 표시.
- 분석·판단 이유: 채택 답변 항목이 추가됐지만 그리드가 3열로 유지되어 네 번째 항목이 자동으로 다음 줄에 배치됐다. 380px 이하에서는 버튼을 다시 키우는 예외 규칙도 있어 함께 정리했다.
- 수정 파일: `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 활동 요약을 동일 너비 4열로 바꾸고 모바일 간격, padding, 숫자·라벨 크기를 축소했다. 최소 클릭 높이는 44px로 유지했다.
- 검증 결과: 프로필 파일 대상 ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-05 프로필 한 줄 소개 제거

- 요청 요약: 프로필 헤더의 한 줄 소개 영역 제거.
- 분석·판단 이유: 현재 소개 저장 데이터가 연결되어 있지 않고 `한 줄 소개가 없습니다.`라는 빈 상태 문구만 반복되어 사용자 식별에 도움이 되지 않았다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 프로필 헤더의 소개 문구와 로딩 skeleton 소개 줄을 제거했다. 소개 전용 말줄임·줄바꿈 CSS도 삭제하고 닉네임과 아이디만 유지했다.
- 검증 결과: 프로필 파일 대상 ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
## 2026-08-05 신뢰 답변자 진행도 UI 개선

- 요청 요약: 한 줄 텍스트로 표시되던 신뢰 답변자 정보를 더 완성도 있는 UI로 개선.
- 분석·판단 이유: 기존에는 등급, 현재 점수, 다음 목표가 동일한 작은 글자로 붙어 있어 중요한 상태와 성장 방향을 빠르게 이해하기 어려웠다.
- 수정 파일: `src/components/profile/ProfileScreen.tsx`, `src/App.css`, `docs/project-records/profile.md`, `docs/project-records/UI.md`, `docs/project-records/UX.md`.
- 핵심 변경 내용: 현재 등급, 큰 포인트 수치, 다음 레벨 진행 바, 남은 점수 순서로 정보 계층을 재구성했다. 레벨별 점수 구간을 기준으로 진행률을 계산하고 progressbar 접근성 속성을 추가했다. 모바일에서는 padding과 글자 크기를 축소한다.
- 검증 결과: 프로필 파일 대상 ESLint와 프로덕션 빌드로 확인했다.
- 남은 작업: 없음.
